# PigeonMesh Source Map

- `Sources/PigeonMesh/Fragmentation.swift` — transport-size fragmentation and
  reassembly.
- `Sources/PigeonMesh/MeshPacket.swift` — packet format, TTL, duplicate
  suppression, and relay decisions.
- `Sources/PigeonMesh/SessionEnvelope.swift` — session message envelope.
- `Tests/PigeonMeshTests/` — package test suite.
