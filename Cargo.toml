[workspace]
members = [
  "pigeon-core",
  "pigeon-ffi",
  "pigeon-mesh",
  "pigeon-relay",
]
resolver = "2"

[profile.release]
# Keep release artifacts small when building from the repository workspace. The
# relay image also builds this workspace with the root Cargo.lock.
strip = true
