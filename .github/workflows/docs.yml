name: Docs

on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - "pigeon-mesh/Cargo.toml"
      - "pigeon-mesh/Cargo.lock"
      - "pigeon-mesh/src/**"
      - "pigeon-relay/Cargo.toml"
      - "pigeon-relay/Cargo.lock"
      - "Cargo.toml"
      - "Cargo.lock"
      - "pigeon-core/Cargo.toml"
      - "pigeon-core/Cargo.lock"
      - "Pigeon/README.md"
      - "Pigeon/CONTRIBUTING.md"
      - "Pigeon/docs/**"
      - "Pigeon/mkdocs.yml"
      - "pigeon-mesh/README.md"
      - "pigeon-mesh/CONTRIBUTING.md"
      - "pigeon-mesh/docs/**"
      - "pigeon-mesh/mkdocs.yml"
      - "pigeon-relay/README.md"
      - "pigeon-relay/CONTRIBUTING.md"
      - "pigeon-relay/docs/**"
      - "pigeon-relay/mkdocs.yml"
      - "pigeon-core/src/**"
      - "CONTRIBUTING.md"
      - "README.md"
      - "mkdocs.yml"
      - "pyproject.toml"
      - "uv.lock"
      - ".github/workflows/docs.yml"
  pull_request:
    branches: [main]
    paths:
      - "docs/**"
      - "pigeon-mesh/Cargo.toml"
      - "pigeon-mesh/Cargo.lock"
      - "pigeon-mesh/src/**"
      - "pigeon-relay/Cargo.toml"
      - "pigeon-relay/Cargo.lock"
      - "Cargo.toml"
      - "Cargo.lock"
      - "pigeon-core/Cargo.toml"
      - "pigeon-core/Cargo.lock"
      - "Pigeon/README.md"
      - "Pigeon/CONTRIBUTING.md"
      - "Pigeon/docs/**"
      - "Pigeon/mkdocs.yml"
      - "pigeon-mesh/README.md"
      - "pigeon-mesh/CONTRIBUTING.md"
      - "pigeon-mesh/docs/**"
      - "pigeon-mesh/mkdocs.yml"
      - "pigeon-relay/README.md"
      - "pigeon-relay/CONTRIBUTING.md"
      - "pigeon-relay/docs/**"
      - "pigeon-relay/mkdocs.yml"
      - "pigeon-core/src/**"
      - "CONTRIBUTING.md"
      - "README.md"
      - "mkdocs.yml"
      - "pyproject.toml"
      - "uv.lock"
      - ".github/workflows/docs.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: Check Docs
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Set up uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install docs dependencies
        run: uv sync --locked --group docs

      - name: Build docs
        run: uv run --group docs mkdocs build --strict

  build:
    name: Build Docs
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
    runs-on: macos-26
    permissions:
      contents: read
      pages: write
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Set up uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install docs dependencies
        run: uv sync --locked --group docs

      - name: Build docs
        run: uv run --group docs mkdocs build --strict

      - name: Build pigeon-mesh API docs
        run: |
          rm -rf "$RUNNER_TEMP/pigeon-mesh-docs" "$PWD/public/api/pigeon-mesh"
          cargo doc \
            -p pigeon-mesh \
            --no-deps \
            --document-private-items \
            --target-dir "$RUNNER_TEMP/pigeon-mesh-docs"
          mkdir -p "$PWD/public/api/pigeon-mesh"
          cp -R "$RUNNER_TEMP/pigeon-mesh-docs/doc" "$PWD/public/api/pigeon-mesh/doc"

      - name: Build relay API docs
        run: |
          rm -rf "$RUNNER_TEMP/pigeon-relay-docs" "$PWD/public/api/pigeon-relay"
          cargo doc \
            --manifest-path pigeon-relay/Cargo.toml \
            --no-deps \
            --document-private-items \
            --target-dir "$RUNNER_TEMP/pigeon-relay-docs"
          mkdir -p "$PWD/public/api/pigeon-relay"
          cp -R "$RUNNER_TEMP/pigeon-relay-docs/doc" "$PWD/public/api/pigeon-relay/doc"

      - name: Build pigeon-core API docs
        run: |
          rm -rf "$RUNNER_TEMP/pigeon-core-docs" "$PWD/public/api/pigeon-core"
          cargo doc \
            -p pigeon-core \
            --no-deps \
            --document-private-items \
            --target-dir "$RUNNER_TEMP/pigeon-core-docs"
          mkdir -p "$PWD/public/api/pigeon-core"
          cp -R "$RUNNER_TEMP/pigeon-core-docs/doc" "$PWD/public/api/pigeon-core/doc"

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: public

  deploy:
    name: Deploy to GitHub Pages
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
