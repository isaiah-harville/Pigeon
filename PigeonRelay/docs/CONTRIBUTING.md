# Contributing to PigeonRelay

Read the root contributor guide first, then use `PigeonRelay/CONTRIBUTING.md`
for relay-specific expectations.

The relay must remain a blind mailbox: no plaintext, no keys, no accounts, and
no content-derived logs.
