[project]
name = "pigeon-docs"
version = "0.1.0"
description = "MkDocs documentation build for the Pigeon monorepo."
requires-python = ">=3.12"
dependencies = [
  "mkdocs>=1.6.1,<2",
  "mkdocs-material>=9.6.0",
  "mkdocs-monorepo-plugin>=1.1.0",
]

[tool.uv]
package = false
